<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">

    <!-- Codificación y severidad general -->
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>

    <!-- Longitud máxima de línea -->
    <module name="LineLength">
        <property name="max" value="185"/>
        <property name="ignorePattern" value="^package.*|^import.*"/>
    </module>

    <!-- Reglas de TreeWalker: analizan estructura de código -->
    <module name="TreeWalker">

        <!-- Cada clase debe declarar package -->
        <module name="PackageDeclaration"/>

        <!-- Imports prohibidos si no se usan -->
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>

        <!-- Nomenclatura básica -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>

        <!-- Espacios y sangría -->
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>
        <module name="NoWhitespaceBefore"/>
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="tabWidth" value="4"/>
        </module>

        <!-- Estilo de llaves -->
        <module name="LeftCurly">
            <property name="option" value="eol"/>
        </module>
        <module name="RightCurly">
            <property name="option" value="same"/>
        </module>

        <!-- Reglas de complejidad simples -->
        <module name="MethodLength">
            <property name="max" value="100"/>
        </module>
        <module name="CyclomaticComplexity">
            <property name="max" value="10"/>
        </module>

    </module>

</module>
